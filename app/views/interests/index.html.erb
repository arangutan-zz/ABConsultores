<h1>Listing interests</h1>

<table>
  <thead>
    <tr>
      <th>Tema</th>
      <th>Informacion</th>
      <th>Stakeholder</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @interests.each do |interest| %>
      <tr>
        <td><%= interest.tema %></td>
        <td><%= interest.informacion %></td>
        <td><%= interest.stakeholder.nombre %></td>
        <td><%= link_to 'Archivo', interest.imagen_url  %></td>
        <td><%= link_to 'Pdf', '/interests/'+interest.id.to_s+'.pdf' , target: '_blank'%></td>
        <td><%= link_to 'Edit', edit_interest_path(interest) %></td>
        <td><%= link_to 'Destroy', interest, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to (image_tag 'crear_nuevo2.png'), new_interest_path %>
<br>
<%= link_to 'PDF', '/interests.pdf', target: '_blank' %>
